const container = {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
}

const shadow = {
    textShadowOffset: { width: -1, height: 1 },
    textShadowRadius: 15,
}

const Styles = {
    container,
    shadow
}

export default Styles